{
  "name": "substack-cli",
  "version": "0.1.0",
  "description": "CLI tool for managing Substack publications",
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "commitlint": "commitlint --from HEAD~1 --to HEAD --verbose"
  }
}
